# Studio Meta plugin disabler

Disable plugins depends on environment.

## Usage

The following server environment variables must be set:
```bash
DISABLE_PLUGINS_[ENVIRONMENT]=example/example1.php
```
